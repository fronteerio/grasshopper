# The Grasshopper Event Engine config module

The config module is responsible for the configuration of Grasshopper apps.
Configuration options include the enabled authentication strategies per app and app.
